/* This is the state of the program and is implemented in javaScript*/

var factory = new CoffeeShopFactory();
var buy ={};

buy.order = factory.createOrder();
buy.order2 = factory.createEmployeeOrder();

